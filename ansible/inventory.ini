# Este repo se aplica sobre la propia maquina (ansible-pull), no por SSH.
[workstations_arch]
localhost ansible_connection=local

# Debian 12 (servidor). Grupo aparte del de Arch: cada playbook apunta al suyo,
# asi que aunque los dos hosts sean 'localhost' nunca se cruzan.
[workstations_debian]
localhost ansible_connection=local
