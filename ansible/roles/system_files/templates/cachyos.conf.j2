# Generado por Ansible. No editar a mano.
# El PARTUUID, el fstype y el microcodigo se detectan de esta maquina.
title   Arch Linux (cachyos)
sort-key arch-cachyos
linux   /vmlinuz-linux-cachyos
{% if ciber_ucode_initrd %}
initrd  /{{ ciber_ucode_initrd }}
{% endif %}
initrd  /initramfs-linux-cachyos.img
options root=PARTUUID={{ ciber_root_partuuid }} {{ boot_kernel_options }} rootfstype={{ ciber_root_fstype }}
